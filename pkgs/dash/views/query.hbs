<script>
  var data = {{{json data}}}
  var columns = {{{json columns}}}
  var title = "{{{name}}}"

  tabs.push({
    title: title,
    type: 'grid',
    selModel: 'row',
    data: data,
    defaults: defaults,
    columns: columns
  })
</script>
